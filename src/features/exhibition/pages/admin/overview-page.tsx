'use client';

import { useState } from 'react';
import { BarChart3, Bell, ClipboardList, Trophy, Users, Zap } from 'lucide-react';
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { results as seedResults } from '../../data/data';
import { Badge } from '../../components/ui';
import { AdminShell, Leaderboard, StatCard } from '../../components/admin';

export function AdminOverview() {
  const [hidden, setHidden] = useState(false); const chart = seedResults.slice(0,5).map(r => ({ name: `#${r.projectId.slice(1)}`, points: r.totalPoints })); const recent = ['Anonymous visitor','Verified teacher','Anonymous student','Verified visitor'];
  return <AdminShell title="Overview"><div className="mb-6 flex items-end justify-between"><div><p className="text-sm text-muted-foreground">Tuesday, May 20, 2025</p><h2 className="mt-1 font-display text-3xl font-bold">Good morning, Amina.</h2></div><Badge tone="gold">Voting is open</Badge></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={Users} label="Registered users" value="32" note="Across all categories" /><StatCard icon={Zap} label="Votes cast" value="23" note="9 voters remaining" tone="gold" /><StatCard icon={Trophy} label="Weighted points" value="128" note="Updated just now" tone="lavender" /><StatCard icon={ClipboardList} label="Active projects" value="12" note="All projects live" /></div><div className="admin-grid mt-4"><div className="col-span-7"><Leaderboard pointsHidden={hidden} setPointsHidden={setHidden} /></div><div className="col-span-5 rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><span className="text-xs font-bold uppercase tracking-[.15em] text-primary">At a glance</span><h2 className="mt-1 font-display text-2xl font-bold">Participation</h2></div><div className="relative grid h-24 w-24 place-items-center rounded-full" style={{ background: 'conic-gradient(#176866 72%, #e7e4da 0)' }}><div className="grid h-16 w-16 place-items-center rounded-full bg-card font-display text-2xl font-bold">72%</div></div></div><div className="mt-6 space-y-3 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Votes cast</span><strong>23</strong></div><div className="flex justify-between"><span className="text-muted-foreground">Registered users</span><strong>32</strong></div><div className="flex justify-between"><span className="text-muted-foreground">Remaining voters</span><strong>9</strong></div></div></div><div className="col-span-7 rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><span className="text-xs font-bold uppercase tracking-[.15em] text-primary">Comparison</span><h2 className="mt-1 font-display text-2xl font-bold">Weighted points</h2></div><BarChart3 className="text-muted-foreground" size={19} /></div><div className="mt-5 h-56"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart} layout="vertical" margin={{ left: 0, right: 15 }}><CartesianGrid horizontal={false} stroke="#e2e2dc" /><XAxis type="number" hide /><YAxis type="category" dataKey="name" width={35} tick={{ fontSize: 11 }} /><Tooltip cursor={{ fill: '#f0efe9' }} /><Bar dataKey="points" fill="#176866" radius={[0,5,5,0]} /></BarChart></ResponsiveContainer></div></div><div className="col-span-5 rounded-2xl border border-border bg-card p-5 shadow-sm sm:p-6"><div className="flex justify-between"><div><span className="text-xs font-bold uppercase tracking-[.15em] text-primary">Just in</span><h2 className="mt-1 font-display text-2xl font-bold">Recent activity</h2></div><Bell size={18} className="text-muted-foreground" /></div><div className="mt-4 space-y-4">{recent.map((name,i) => <div key={name} className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-full bg-secondary text-xs font-bold text-primary">{name.split(' ').map(s => s[0]).join('')}</span><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{name}</p><p className="text-xs text-muted-foreground">{['Visitor','Teacher','Student','Visitor'][i]} · {i+1}m ago</p></div><Badge tone="teal">Recorded</Badge></div>)}</div></div></div></AdminShell>;
}
