'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { ClipboardList, Edit3, Filter, Plus, Search } from 'lucide-react';
import { projects as seedProjects, results as seedResults } from '../../data/data';
import { mockServices } from '../../data/services';
import { Badge, Button, EmptyState, Modal, Toast, cx, useToastMessage } from '../../components/ui';
import { AdminShell, PageIntro } from '../../components/admin';

export function AdminProjects() {
  const router = useRouter(); const [query, setQuery] = useState(''); const [view, setView] = useState<'grid'|'table'>('grid'); const [archived, setArchived] = useState(false); const [archiveId, setArchiveId] = useState(''); const { notify, message, clear } = useToastMessage(); const visible = seedProjects.filter(p => p.title.toLowerCase().includes(query.toLowerCase()) && p.isArchived === archived);
  return <AdminShell title="Projects"><PageIntro eyebrow="Exhibition catalogue" title="Project management" action={<Button onClick={() => router.push('/admin/projects/new')}><Plus size={17} /> Add project</Button>} /><div className="mb-5 flex flex-wrap gap-2"><div className="relative min-w-[220px] flex-1"><Search size={16} className="absolute left-3 top-3.5 text-muted-foreground" /><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search projects" className="h-11 w-full rounded-xl border border-border bg-card pl-9 text-sm" data-testid="input-admin-project-search" /></div><select value={archived ? 'Archived' : 'Active'} onChange={e => setArchived(e.target.value === 'Archived')} className="h-11 rounded-xl border border-border bg-card px-3 text-sm" data-testid="select-project-status"><option>Active</option><option>Archived</option></select><div className="flex rounded-xl border border-border bg-card p-1"><button onClick={() => setView('grid')} className={cx('rounded-lg px-3 text-xs font-bold', view === 'grid' && 'bg-primary text-white')} data-testid="button-grid-view">Cards</button><button onClick={() => setView('table')} className={cx('rounded-lg px-3 text-xs font-bold', view === 'table' && 'bg-primary text-white')} data-testid="button-table-view">Table</button></div></div>{view === 'grid' ? <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{visible.map(p => <div key={p.id} className="overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="relative aspect-[2]"><img src={p.imageUrl} alt="" className="h-full w-full object-cover" /><span className="absolute left-3 top-3 rounded-md bg-[#25334d]/80 px-2 py-1 text-xs font-bold text-white">{p.projectNumber}</span></div><div className="p-4"><div className="flex justify-between gap-2"><h3 className="font-display text-xl font-bold">{p.title}</h3><Badge>{seedResults.find(r => r.projectId === p.id)?.totalVotes || 0} votes</Badge></div><p className="mt-1 text-sm text-muted-foreground">{p.teamName} · {p.category}</p><div className="mt-4 flex gap-2 border-t border-border pt-3"><Button variant="outline" className="flex-1" onClick={() => router.push(`/admin/projects/${p.id}/edit`)}><Edit3 size={15} /> Edit</Button><Button variant="quiet" onClick={() => setArchiveId(p.id)}><Filter size={15} /> Archive</Button></div></div></div>)}</div> : <div className="overflow-auto rounded-2xl border border-border bg-card"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-border text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="p-4">Project</th><th className="p-4">Category</th><th className="p-4">Team</th><th className="p-4">Votes</th><th className="p-4">Actions</th></tr></thead><tbody>{visible.map(p => <tr key={p.id} className="border-b border-border last:border-0"><td className="p-4 font-bold">{p.projectNumber} · {p.title}</td><td className="p-4"><Badge>{p.category}</Badge></td><td className="p-4 text-muted-foreground">{p.teamName}</td><td className="p-4">{seedResults.find(r => r.projectId === p.id)?.totalVotes}</td><td className="p-4"><button onClick={() => router.push(`/admin/projects/${p.id}/edit`)} className="font-bold text-primary" data-testid={`button-edit-project-${p.id}`}>Edit</button></td></tr>)}</tbody></table></div>}{!visible.length && <EmptyState icon={<ClipboardList />} title="No projects in this view" text="Add a project or change the filter." />}{archiveId && <Modal onClose={() => setArchiveId('')}><div className="grid h-12 w-12 place-items-center rounded-xl bg-destructive/10 text-destructive"><Filter /></div><h2 className="mt-5 font-display text-2xl font-bold">Archive this project?</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">It will disappear from the active exhibition. This demo action can be connected to the project API later.</p><div className="mt-6 grid grid-cols-2 gap-3"><Button variant="quiet" onClick={() => setArchiveId('')}>Cancel</Button><Button variant="danger" onClick={async () => { await mockServices.archiveProject(archiveId); setArchiveId(''); notify('Project archived in the demo'); }}>Archive project</Button></div></Modal>}{message && <Toast message={message} onClose={clear} />}</AdminShell>;
}
